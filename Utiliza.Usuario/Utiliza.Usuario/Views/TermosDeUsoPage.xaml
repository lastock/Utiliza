<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Utiliza.Usuario.Views.TermosDeUsoPage"
             Title="{Binding Title}">

    <StackLayout>
        <Label Text="Termos e Condições" TextColor="Black" HorizontalOptions="Center" FontSize="Large" FontAttributes="Bold" Margin="20,20,10,20"/>
        <Label Text="{Binding Texto1}" TextColor="Black" Margin="20,0,10,10"/>
        <Label Text="{Binding Texto2}" TextColor="Black" Margin="20,0,10,10"/>
        <Label Text="{Binding Texto3}" TextColor="Black" Margin="20,0,10,10"/>

    </StackLayout>

</ContentPage>